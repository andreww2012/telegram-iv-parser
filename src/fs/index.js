const {initStructure} = require('./init');
const {addFile} = require('./add-file');
const {createDir} = require('./create-dir');
const {readSite} = require('./read-site');

module.exports = {
  initStructure,
  addFile,
  createDir,
  readSite,
};
